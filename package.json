{
  "name": "rabbit-r1-local-companion",
  "version": "1.0.0",
  "description": "Local-first productivity companion for Rabbit R1 running with local Ollama",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "cert": "openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -sha256 -days 365 -nodes -subj '/CN=localhost'"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
