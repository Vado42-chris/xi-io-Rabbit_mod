{
  "name": "xi-io-ibal",
  "version": "1.0.0",
  "description": "Local-first user-facing orchestration and ingress app for the xi-io ecosystem (ibal console)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/server.test.js",
    "cert": "openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -sha256 -days 365 -nodes -subj '/CN=localhost'"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
